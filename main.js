let output = 0; //current screen value

function add (a, b) {
    output = a + b;
}

function subtract (a, b) {
    output = a - b;
}

function multiply (a, b) {
    output = a * b;
}

function divide (a, b) {
    output = a / b;
}